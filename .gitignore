# Build files
.gradle
#.vscode
.kotlin
build

# IDE files
*.iml
.idea/*
!.idea/icon.png
/captures
.history

# Configuration files
local.properties

# macOS specific files
.DS_Store

/app/client_secrets.json
/app/google-services.json
*.jks
# HONDANA: the release key is committed only encrypted (signing/hondana-release.tar.gpg);
# CI decrypts it into these files, which must never be committed. See signing/README.md.
signing/signing.properties
app/standard/preview/*
